# Training, calibration, and backtesting dependencies.
# Browser inference is dependency-free — these are only needed if you
# want to retrain (train_*.py) or run the backtester (backtest.py).

torch>=2.0.0
numpy>=1.24.0
yfinance>=0.2.30
scikit-learn>=1.3.0
xgboost>=2.0.0
