{
  "name": "market-analysis-push-alerts",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: cron-driven Web Push price alerts (closed-tab) for Market Analyzer.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
