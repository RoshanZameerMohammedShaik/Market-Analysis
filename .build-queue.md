# Build Queue — pending verification

Steps committed and pushed but not yet live-tested. Verify all in one
sweep once the GitHub Pages CDN edge refreshes (~10min after each
push).

## Built and shipped (awaiting test)

### Step 0.3.1 — harden framing rules (commit 8331484)
- Trivial answer (zero, no action) = stop there, do not compute alternatives
- First sentence states answer (repeated in FRAMING + RESPONSE SHAPE)
- **Test:** Re-ask the WIZC3 break-even question. Expect:
  - Lead with "zero" / "you don't need to invest more"
  - Show x = 0 derivation as proof
  - STOP — no hypothetical "but if you wanted X% profit" extrapolation
  - 0 unverified flags

### Step 1 — probable range alongside possible (commit 1204154)
- analysis.js returns probableHigh/Low alongside predictedHigh/Low; ATR × 0.18..0.65, asymmetric per signal, confidence-scaled
- Mia CONTEXT shows both bands; prompt: lead with probable, possible for risk
- UI: new "Probable" pill above the 3-card grid; existing cards renamed to "Possible High/Low"
- CSS theme-aware via color-mix on var(--accent); mobile fallback
- **Test:** Load AAPL → signal section shows "Probable" strip with narrow range (e.g. ~3% spread) above existing wider range (~6-8%). Theme-tinted accent. Mia, asked "what's the probable target?", quotes the narrow band.

## Building next


### Step 2 — invert confidence scaling — MERGED INTO STEP 1
- The probable band added in Step 1 already has the inverted scaling (high confidence → narrower probable, low confidence → wider). The possible band intentionally retains the original scaling because it represents a risk envelope (high-confidence directional call SHOULD reach farther in the called direction; that's not a bug, it's the point of "possible"). No separate Step 2 commit needed.

### Step 3 — Layer 1 ledger write side (commit 573a8f3)
- ledger_universe.py: 620+ symbol union, region_for() suffix detection
- record_predictions.py: per-region open-of-day signal logger, idempotent
- record_outcomes.py: fills +1/+3/+5/+10/+20 horizons as each matures
- recalibrate_from_ledger.py: writes model/live_calibration.json
- .github/workflows/live-ledger.yml: 7 region crons + crypto snapshot + nightly resolve+recalibrate
- **Test:** Trigger `Run workflow` → predict-NYSE manually. Confirm `model/ledger/2026.jsonl` gets ~340 rows committed back to the repo. Run `resolve-outcomes` after a few days; horizon slots fill in. Run `recalibrate` to produce `model/live_calibration.json`.
- **Note:** Pure data collection. Engine doesn't USE this data until Step 4 wires the read side.

### Step 4 — Layer 1 ledger read side + Mia tools (commit a2e5388)
- calibration.js: live ledger now top priority (n≥30 in bucket) above all backtest strata; getCalibrationSource() exposes which strata answered
- ui-bridge.js: readLedgerHistory + readLiveCalibration with 5min cache
- 2 new Mia tools: get_ledger_history, get_live_calibration
- Deep-dive prompt now includes ledger-history step between signal read and research bundle
- ACTION_VERBS entries for the new tools
- **Test:** Once Step 3's cron has populated `model/ledger/2026.jsonl` and `model/live_calibration.json`, ask Mia "what's the live calibration?" → expects her to call get_live_calibration and report rates. Ask "how has the engine done on NVDA recently?" → calls get_ledger_history with symbol=NVDA, reports 1d hit rate.
- **Note:** No behavior change today (0 ledger rows). Activates progressively as data accumulates.

### Step 5 — empirical ranges (3 hours, but only after ~3 months of ledger data) — DEFERRED
- This is what replaces the ATR formula in calculatePriceTargets with empirical distributions from live ledger outcomes. Cannot meaningfully build until ledger has months of resolved horizons.
- **Action:** Build when Step 3's cron has been running 90+ days. Logged in queue, no commit yet.

### Step 6 — UI polish (commit f6f43a0)
- Header pulse: replaced SVG ECG squiggles with 8-bar candlestick pulse, theme-tinted via var(--accent), mobile cascade, reduced-motion respected
- Live calibration badge: new 'live' state on .cal-badge with accent pulse animation; renders when getCalibrationSource() returns live-*
- Theme audit: Step 1's .probable-strip and this commit's badge both use color-mix on var(--accent), no per-theme overrides
- **Test:** Visit live page → header has trading-bar pulses (no squiggles). Switch theme (dark/light/aurora) → bars pick up theme accent color. Once ledger has data, signal header shows "◉ live" badge instead of "✓ calibrated".

## Build summary

7 commits pushed in sequence (Step 0.3.1 → 1 → 3 → 4 → 6, with 2/5 absorbed/deferred). Total LOC delta: ~1000 added.

CDN cache TTL is 600s, so all changes are live ~10 min after the last push (f6f43a0). After that all tests in this queue can be run end-to-end on roshanzameermohammedshaik.github.io/Market-Analysis.


